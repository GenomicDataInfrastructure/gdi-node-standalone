# NEGATIVE FIXTURE — missing mandatory field (`description`).
# Violates: gdi-metadata `DatasetShape#description` `sh:minCount 1` (and the
# Rust gate's "description is mandatory and must not be empty"). The field is
# absent entirely.
# Both encodings enforce this minCount, so it is a clean dual-encoding agreement
# case (REJECT / VIOLATE on both sides).
# Expected: Rust gate REJECTS; pySHACL VIOLATES.
metadata:
  prefix: "GDI"
  org: "EXAMPLE"
  catalog: "gdi-aggregated"
  title:
    en: "Genome of Europe Estonia aggregated allele frequencies"
    et: "Genome of Europe Eesti koondsagedused"
  accessRights: "http://publications.europa.eu/resource/authority/access-right/PUBLIC"
  applicableLegislation:
    - "http://data.europa.eu/eli/reg/2025/327/oj"
  license: "https://creativecommons.org/licenses/by/4.0/"
  creator:
    - name: "Genome of Europe - EE node"
  healthCategory:
    - "http://data.gdi.eu/core/p2/HealthCategoryHumanGenomic"
  keywords:
    - allele-frequency
    - genomics
  numberOfUniqueIndividuals: 1234
  conformsTo:
    - "http://data.gdi.eu/core/p2/1MGCompliant"
  legalBasis:
    - "https://w3id.org/dpv#Consent"
  contactPoint:
    fn: "Data team"
    hasEmail: "mailto:data@example.org"
    hasURL: "https://example.org"

files:
  - category: "VCF"
    reference: "GRCh38"
    files:
      - "relative/to/yaml/file1.vcf.gz"

config:
  mode: aggregated
  blockRange: 10000000
  afSource: "The Genome of Europe"
  afSourceReference: "https://genomeofeurope.eu/"
  minAlleleCount: 0
